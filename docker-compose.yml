services:
  job-fit-agent:
    build: .
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./applications:/app/applications
    command: ["python", "-m", "job_fit_agent.main", "digest"]
